<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.start2">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Start2"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".home.NavigatorActivity"
            android:exported="false"
            android:label="@string/title_activity_navigator"
            android:theme="@style/Theme.Start2" />
        <activity
            android:name=".home.HomeHostActivity"
            android:exported="false" />
        <!-- WelcomeActivity, initial activity -->
        <activity android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainHostActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Start2">
        </activity>

        <activity android:name=".RegistrationActivity"></activity>
    </application>
</manifest>
